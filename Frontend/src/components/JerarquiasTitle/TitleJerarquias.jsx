import './TitleJerarquias.css';

export function TitleJerarquias({title}) {
    return (
        <section className="title-Jerarquias">
            <h1 className="title-ja">{title}</h1>
        </section>
    );
}