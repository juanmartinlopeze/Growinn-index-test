# Variables de entorno para el servidor

# Puerto del servidor
PORT=3000

# Entorno de ejecución (development, production)
NODE_ENV=development

# URLs permitidas para CORS (separadas por comas)
ALLOWED_ORIGINS=https://localhost:5173,https://localhost:3000,http://localhost:5173,http://localhost:3000

# Configuración SSL
SSL_CERT_PATH=./certs/cert.pem
SSL_KEY_PATH=./certs/key.pem

# Configuración de Supabase (estas deberían estar definidas)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_KEY=
